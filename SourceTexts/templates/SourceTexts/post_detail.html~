<html>
{% block content %}
        <h1>{{ post.title }}</h1>
        <h2>{{ post.text}}</h2>
        <p>{{ post.from_language}}</p>
        <p>{{ post.to_language}}</p>
        <p>{{ post.translation}}</p>
        <p>{{ post.alignment}}</p>
        <p>{{ post.frequency_list_exists}}</p>
{% endblock %}
</html>
